{{ form_start(form) }}
    {{ form_widget(form) }}
	<a class="p-t-10 btn btn--pill btn--green">
		<button class="btn">{{ button_label|default('Créer') }}</button>
	</a>
	{% if msg %}
		<div class="alert alert-success">
			{{msg}}
		</div>
	{% endif %}
{{ form_end(form) }}
