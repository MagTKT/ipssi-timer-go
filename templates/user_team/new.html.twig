{% extends 'base.html.twig' %}

{% block title %}New UserTeam{% endblock %}

{% block body %}

    <div class="page-wrapper bg-gra-01 p-t-180 p-b-100 font-poppins">
        <div class='contain-block'>
            <div class="wrapper wrapper--w780">
                <div class="button">
                    <a class='btn btn-info' href="{{ path('team_show', {'id': idTeam.id}) }}">Retour</a>
                </div>
                {{ include('user_team/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
