{% extends 'base.html.twig' %}

{% block title %}UserTeam{% endblock %}

{% block body %}
    
    <div class="page-wrapper bg-gra-01 p-t-180 p-b-100 font-poppins">
        <div class="contain-block">
            <div class="wrapper wrapper--w780">
                <div class="button">
                    <a class='btn btn-info' href="{{ path('user_team_index') }}">Retour à la liste</a>
                </div>
                    <div class="card card-3">
                        <div class="card-body">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>Id</th>
                                        <td>{{ user_team.id }}</td>
                                    </tr>
                                    <tr>
                                        <th>Date_creation</th>
                                        <td>{{ user_team.DateCreation ? user_team.DateCreation|date('Y-m-d H:i:s') : '' }}</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                <div class='button'>
                    <a class='btn btn-info' href="{{ path('user_team_edit', {'id': user_team.id}) }}">Editer</a>
                    {{ include('user_team/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
