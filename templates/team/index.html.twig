{% extends 'base.html.twig' %}

{% block title %}Team index{% endblock %}

{% block body %}
    <div class="page-wrapper bg-gra-01 p-t-180 p-b-100 font-poppins">
        <div class="contain-block">
        <h1>Team index</h1>
        <div class="wrapper wrapper--w780">
            <div class='url card-3bis'><a href="{{ path('team_new') }}">Creer une nouvelle team</a></div>
            <div class="card card-3">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Name</th>
                                <th>Proprietaire</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for team in teams %}
                                <tr>
                                    <td>{{ team.id }}</td>
                                    <td>{{ team.name }}</td>
                                    <td>{{ team.TeamAdmin.name }} {{ team.TeamAdmin.lastName }}</td>
                                    <td>
                                        <a class='color_one' href="{{ path('team_show', {'id': team.id}) }}">Détail</a>
                                        <a class='color_two' href="{{ path('team_edit', {'id': team.id}) }}">Editer</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3">no records found</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        </div>
    </div>
{% endblock %}
